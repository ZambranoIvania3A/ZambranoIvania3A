
import {Empleado} from './Herencia/Empleado'
import {Estudiante} from './Herencia/Estudiante'
import {Profesor} from './Herencia/Profesor'
import {PersonalServicio} from './Herencia/PersonalServicio'

export class Empresa {
public empleados: Empleado[];
public estudiantes: Estudiante[];
public profesores: Profesor[];
public personalServicio: PersonalServicio[];

constructor() {
    this.empleados=[];
    this.estudiantes=[];
    this.profesores=[];
    this.personalServicio=[];
}


public añadirEmpleado; { 
    nombre: String;
    apellido: String;
    estadoCivil: String;
    anioIncorporacion: Number;
    numeroDespacho: String;
}
void {


        const empleado = new Empleado(  
            nombre,
            apellidos,
            estadoCivil,
            anioIncorporacion,
            numeroDespacho,
        );
        this.empleados.push(Empleado);
    }


    public agregarPersonal Servicio(
    nombre: string,
    apellidos: string, 
    identificacion: string, 
    estadoCivil: string,
    seccion: string 
    void (
        
    const personalservicio = new PersonalServicio{ 
        
        nombre,
        apellidos, 
        Identificacion,
        estadoCivil,
        seccion
        
        const person this.personalservicio.push(personalservicio
        
        public primirInformacion(): vold ( console.log("Empleados (cantidad: this.empleados.forEach((empleado)-> ( empleado.imprimirInformacion(); console.log("");
        
        console.log("Estudiantes (cantidad: this.estudiantes.forEach((estudiante) estudiante. ImprimirInformacion(); console.log("");
        
        });
        
        console.log("Profesores (cantidad);
        
        this.profesores.forEach (profesor);
        



    }
   }
}
